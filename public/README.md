Place your PMB poster image here.

The PMB page (`src/pages/PMB.tsx`) references `/pmb-poster.jpg` as the poster source. Replace `public/pmb-poster.jpg` with the actual poster file (same name) or update the path in the component accordingly.

You can also replace this README with the actual image file.
